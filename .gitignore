venv/
__pycache__/
*.pyc
.DS_Store

# LaTeX build artifacts
doc/*.aux
doc/*.bbl
doc/*.bcf
doc/*.blg
doc/*.log
doc/*.out
doc/*.run.xml
doc/*.toc

# debug generated outputs
diagnostics/out_*.png
diagnostics/report.tex
diagnostics/report.pdf

# per-run artifacts: ignore checkpoints and logs, but keep configs
runs/*/model.pt
runs/*/training_log.jsonl
runs/latest
runs/*.log

# stray top-level logs
*.log
runs_comparison.png

# checkpoints dir (old location)
checkpoints/

# misc generated outputs
training_log.jsonl
results.json
src/clustering_visualisation.png

# data and assignment (provided externally)
to_share/data/
to_share/Case\ Study\ Assignment.pdf

# private summary, kept locally but not committed
doc/summary.tex
doc/summary.pdf
doc/claude_opus_thoughts.md

# presentation and prep notes (local only)
doc/presentation.tex
doc/presentation.pdf
doc/presentation.nav
doc/presentation.snm
doc/approach_backup.pdf
doc/prep_notes.tex
doc/prep_notes.pdf
